class enemy {
  constructor(typeOfEnemy) {
    this.typeOfEnemy = typeOfEnemy;

    // Moving speed
    // HP
    // Does it have a shield for some specific attacks (anti-freezing etc)?
    // How many of them will be spawn?
  }

  draw() {}

  update(deltaTime) {
    // Since enemy will be moving all the time, we need to update it every tick
  }
}
