export default class tower {
  constructor(typeOfTower) {
    this.typeOfTower = typeOfTower;

    // Name

    // Circular range

    // Damages

    // Attack speed

    // Kinds(Freezing, group damage, single target damage etc)

    // Price
    var price = 0;
    // Upgradable(?)
    var upgradable = false;
  }

  update() {
    // Since towers don't move, so there is no need for it to update
  }

  draw(context) {}
}
